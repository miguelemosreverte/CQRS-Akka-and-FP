version: "3"

services:
  # web:
  #  build: .
  #  ports:
  #    - "8000:8000"
  db:
    image: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: postgres